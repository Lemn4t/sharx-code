## Release v1.6.5

**Release Date:** June 18, 2026

Патч **v1.6.5** относительно **v1.6.4**: IP-limit job больше не перезапускает все ноды при проверке онлайна; полная поддержка `happ://routing/onadd/` в subscription header (follow-up к #15).

**Миграция БД:** нет.

---

### Bug Fixes

- **Рестарт всех нод из-за IP-limit / session block** (`web/service/xray.go`, `web/service/client_session_block.go`, `xray/session_ip_block_routing.go`)
  - **Симптом:** в логах ноды `session-ip-block-routing: proto: cannot parse invalid wire-format data`, сразу за ним `apply-config` → `compare result=changed` → рестарт Xray при тех же инбаундах.
  - **Причина 1:** job IP-limit (`CheckClientIPLimitJob`) на каждом цикле снова вызывал hot-push для **уже заблокированных** IP.
  - **Причина 2:** при ошибке hot routing панель делала **fallback на полный `apply-config`** на все упавшие ноды — routing rules в payload отличались от running core → лишний restart storm.
  - **Фикс:** не повторять hot-push для IP, уже в `client_blocked_session_ips`; убран `apply-config` fallback; сборка routing rule для `RoutingService.AddRule` через `infra/conf.ToCidrList` (тот же путь, что JSON routing в конфиге).

- **Happ `onadd` в HTTP subscription routing header** (`web/service/subscription_routing_header.go`) — [#16](https://github.com/konstpic/sharx-code/pull/16) от **[@Lemn4t](https://github.com/Lemn4t)** (follow-up к [#15](https://github.com/konstpic/sharx-code/pull/15))
  - Пресет `happ_onadd` в UI (#15) раньше не попадал в бэкенд: заголовок routing в подписке всегда был `happ://routing/add/…`.
  - Теперь `routingDeepLinkPrefixForProfile` отдаёт `happ://routing/onadd/`; normalize не ломает уже готовые onadd-ссылки.

### Operational Notes

- Обновляется **панель (`sharx`)**; **`sharxnode`** желательно обновить вместе (общий `xray` пакет для hot routing API).
- После деплоя: при включённом **IP limit** ноды не должны рестартовать каждые N секунд; в логах панели — warning вместо каскада `apply-config`.
- Если hot routing на старой ноде всё ещё падает (нет `RoutingService` в api.services) — блокировка IP вступит при следующем штатном sync конфига, без рестарта по кругу.

### Test plan

- [ ] Settings → IP limit включён, клиент с лишними online IP → block enforcement срабатывает
- [ ] Логи ноды: нет повторяющихся `apply-config` / `action=restart` каждые 30s при стабильном конфиге
- [ ] Settings → Routing profiles → `happ_onadd` → subscription HTTP header `routing:` содержит `happ://routing/onadd/…`

### 🐳 Docker Images

Images are available in Harbor:

- **sharx**: {{SHARX_TAG_VERSION}} / {{SHARX_TAG_LATEST}}
- **sharxnode**: {{SHARXNODE_TAG_VERSION}} / {{SHARXNODE_TAG_LATEST}}
- **postgres**: {{HARBOR_HOST}}/{{HARBOR_PROJECT}}/postgres:16-alpine / {{HARBOR_HOST}}/{{HARBOR_PROJECT}}/postgres:latest

### 📦 Upgrade from v1.6.4

1. Бэкап `.env` и БД (миграций нет).
2. Обновите **`sharx`** и **`sharxnode`** до **`1.6.5`** / **`latest`**.

```yaml
services:
  sharx:
    image: {{SHARX_TAG_LATEST}}
  sharxnode:
    image: {{SHARXNODE_TAG_LATEST}}
  postgres:
    image: {{HARBOR_HOST}}/{{HARBOR_PROJECT}}/postgres:latest
```

### 🙏 Contributors

- **[@Lemn4t](https://github.com/Lemn4t)** — [#16](https://github.com/konstpic/sharx-code/pull/16) backend `happ://routing/onadd/` в subscription routing header.
- **[@konstpic](https://github.com/konstpic)** — fix IP-limit restart storm, session IP block hot routing.

### Changes (git)

- Сводка: `git diff v1.6.4 v1.6.5 --stat`
- Полный diff: `git diff v1.6.4 v1.6.5`
- Коммиты после **v1.6.4**: `git log v1.6.4..v1.6.5 --oneline`
